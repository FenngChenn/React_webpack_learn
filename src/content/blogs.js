import React, {Component} from 'react';

class Blogs extends Component {
  render() {
    return(
      <div>
        <h2>React</h2>
        <div>React是React类库的入口。如果你正在使用预编译包的其中一个，那么React可能是一个全局变量；如果你使用的是CommonJS模块，你可以用require()引入React。</div>
      </div>
    );
  }
}
